use anyhow::Result;

use crate::utils::*;

use super::super::AocDay;

pub struct Day {}

impl Day {
    pub fn new() -> Self {
        Self {}
    }
}

impl AocDay for Day {
    fn run_part1(&mut self, _input: &[String]) -> Result<AoCResult> {
        Ok(AoCResult::None)
    }

    fn run_part2(&mut self, _input: &[String]) -> Result<AoCResult> {
        Ok(AoCResult::None)
    }
}
